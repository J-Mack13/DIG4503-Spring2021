const HomePage = (props) => {

  let anotherExample ={
    examplePropertyName: "this is a thing!"
  };

  return (
    <div>
      <p>{props.firstName}</p>
    </div>
  );
};

export default HomePage;
